name: Unzip Workflow

on: [push] # Or your preferred trigger

jobs:
  unzip_job: # You can name this anything
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Unzip nightly-patched.zip
        run: unzip -qo nightly-patched.zip
